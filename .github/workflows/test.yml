on:
  release:
    types: [ "published" ]
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Lint
      run:  just lint

  typecheck:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Typecheck
      run:  just typecheck